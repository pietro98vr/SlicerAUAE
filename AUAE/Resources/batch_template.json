{
  "_about": "Batch template for AUAE. Fill 'volumes' with input CBCT/CT paths in the order you want them processed. Each volume is segmented, post-processed, optionally extended, and written to its own subfolder under 'output_root'.",
  "_fields": {
    "mode": "'segment' (default) segments the CT/CBCT volumes; 'extend' extends existing airway segmentation files listed in 'volumes' (no model or GPU needed).",
    "output_root": "Folder where a per-volume subfolder is created. Leave empty to be asked at run time.",
    "subfolder_from": "How each output subfolder is named: 'filename' = the input file stem.",
    "export_formats": "Any of STL, OBJ, NIFTI (upstream export). NIFTI writes the segmentation labelmap.",
    "remove_small_islands": "Drop connected components smaller than min_island_mm3 (upstream cleanup, reimplemented without the editor).",
    "keep_largest_island": "Keep only the single largest component. Off by default.",
    "min_island_mm3": "Small-island threshold in mm^3. Upstream default is 0.3^3 * 200 = 5.4.",
    "segment_external_air": "Also segment the ambient air in front of the face as a second segment (flow-modelling inlet). Off by default.",
    "merge_external_air": "When segment_external_air is true, merge that air into the airway to form a single fluid-domain segment instead of two segments.",
    "smoothing_factor": "Closed-surface smoothing factor 0-1 for the exported mesh (0 = raw voxels). Default 0.",
    "extension": "Our airway extension for flow modelling: repeats the terminal slice to extend the mask.",
    "extension.enabled": "true to extend the final mask.",
    "extension.direction": "Inferior (neck) | Superior (cranial).",
    "extension.length_mm": "Extension length in millimetres.",
    "volumes": "Ordered input paths. In 'segment' mode: CT/CBCT volumes. In 'extend' mode: saved airway segmentation labelmaps (.nii.gz/.nrrd)."
  },
  "mode": "segment",
  "output_root": "",
  "subfolder_from": "filename",
  "export_formats": ["STL", "NIFTI"],
  "remove_small_islands": true,
  "keep_largest_island": false,
  "min_island_mm3": 5.4,
  "segment_external_air": false,
  "merge_external_air": false,
  "smoothing_factor": 0.0,
  "extension": {
    "enabled": false,
    "direction": "Inferior (neck)",
    "length_mm": 100.0
  },
  "volumes": [
    "C:/path/to/patient01_CBCT.nrrd",
    "C:/path/to/patient02_CBCT.nii.gz"
  ]
}
